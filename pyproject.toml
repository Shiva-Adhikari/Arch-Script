[project]
name = "arch-script"
version = "0.1.0"
description = "Post-install automation tool for Arch Linux"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click>=8.1.8",
]

[project.scripts]
arch-script = "main:main"
